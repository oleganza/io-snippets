#!/usr/bin/env io
# oleganza
# July, 3 2008
# Erlang-like PIDs with messagebox

Erl := Object clone 
Erl do(
  pid := method(
    
  )
)

Timer := Erl pid(t,
  receive(
   [pid, "message"],
    
  ) after(t,
    
  )
  
)
